# UI Log 2026-05-27

## Status
- Current review phase: top menu cleanup and export testing.
- Latest UI experiments are in `_template.html` only.
- Do not sync new template changes to song sheets until user confirms the design.
- Do not run `update_index.py`, commit, or push while still iterating UI.

## Today Completed
- Added UI workflow note to `AGENTS.md`.
- Top menu review:
  - Switched menu text toward regular/light text styles.
  - Lowercased menu labels.
  - Added subtle letter spacing.
  - Added flexible space before `bandsheet by ti.muse v6.01`.
  - Changed brand/version to grey tone.
- Added `save jpg` button in `_template.html`.
- Added continuous long JPG export flow:
  - Uses current sheet content.
  - Removes edit controls from exported image.
  - Includes song title, artist, key, bpm, time, vocalist header.
- Tested with `Jesus of Suburbia` as a long song:
  - 13 sections.
  - 380 bars.
  - Test page height around 3645px.
  - `save jpg` button worked and showed `JPG saved`.

## Important Notes
- A temporary preview file was created at `/private/tmp/bandsheet-jpg-test.html`.
- A temporary local preview server was started on `http://127.0.0.1:8765/`.
- The browser is currently on a temporary test URL, not the real repo page.
- Downloaded JPG files go to the browser default download folder, usually `~/Downloads`.

## Continue Work
Recommended: start a new chat for the next phase.

Suggested opening prompt:
```text
Continue Bandsheet v6.01 UI from /Users/ti_am1/Vaults/ti.muse/bandsheet.
Read AGENTS.md and UI_LOG_2026-05-27.md first.
We are starting the next phase: bar and lyric visibility.
Important workflow: edit only _template.html while reviewing UI. Do not sync to song sheets until I confirm. Do not run update_index.py or push until I say go online.
```

## Next Phase Ideas
- Review visibility rules for bar controls in rehearsal vs edit mode.
- Review lyric/note section visibility and readability.
- Decide which controls are always visible, hover-only, edit-only, or hidden in main display.
- Test changes on `_template.html` first.

## Commands For Later
After user confirms design:
```bash
python3 update_index.py
bash push.sh "update: bandsheet v6.01 UI"
```

Do not run these during UI review.
